import 'package:flutter/material.dart';

class ExpanseTransactionPage extends StatelessWidget {
  const ExpanseTransactionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
